#pragma once

namespace tiledkernel {
    /**
     * @brief Scheduler class.
     * The Scheduler class schedules and generates high-performance programs
     * for graphs based on nested loop data structures.
     */
    class Scheduler {};
}  // namespace tiledkernel
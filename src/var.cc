#include "var.hpp"
#include <fmt/core.h>

namespace tiledkernel {
    // std::string IterVar::map() {
    //     return fmt::format("for (int {0} = {1}; {0} < {2}; {0} += {3}) {{\n",
    //                        name, start, end, step);
    // }
}  // namespace tiledkernel